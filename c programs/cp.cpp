#include<bits/stdc++.h>
using namespace std;
#define debug 0
int main(){
    int cases;
    scanf("%d",&cases);
    while(cases--){
        int n,m,q;
        cin >> n >> m >> q ;
        int task[n];
        for(int i=0;i<n;i++){
            cin >> task[i];
        }
        int dead[n];
        for(int i=0;i<n;i++){
            cin >> dead[i];
        }
        int epi[m];
        for(int i=0;i<m;i++){
            cin >> epi[i];
        }
        int time[q];
        for(int i=0;i<q;i++){
            cin >> time[i];
        }
        
    }
}